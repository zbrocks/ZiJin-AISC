{
  "name": "zjin-aisc",
  "version": "1.0.0",
  "description": "ZiJin A.I.S.C. 赛事网站",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "mkdir -p dist/css && node build/css-builder.js",
    "build-js": "mkdir -p dist/js && node build/js-builder.js",
    "build": "npm run build-css && npm run build-js",
    "optimize": "npm run build"
  },
  "keywords": ["zjin", "aisc", "competition"],
  "author": "ZiJin Team",
  "license": "MIT",
  "devDependencies": {
    "cssnano": "^7.0.0",
    "terser": "^5.0.0",
    "postcss": "^8.0.0",
    "glob": "^10.0.0"
  }
}